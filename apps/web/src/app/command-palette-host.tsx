// Skeleton. Full command palette content lands in task-18.
// The store (useCommandPaletteStore) is already live — task-18 can flip it from ⌘K.
export function CommandPaletteHost() {
  return null;
}
