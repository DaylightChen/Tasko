# Execution Log — Task 04: Frontend shell + router + sidebar + Settings

**Scope:** `project`

## Iteration 1

### Implement

**Files created (full list from implementer's scope of work):**
- `apps/web/src/app/` — `error-boundary.tsx`, `theme-bootstrap.tsx`, `sse-connector.tsx` (stub), `hotkey-provider.tsx` (stub), `command-palette-host.tsx` (stub)
- `apps/web/src/api/` — `client.ts` (`apiCall<TOut>` + `ApiError` + `getTabId`), `keys.ts` (query keys factory), `config.ts`, `projects.ts`, `folders.ts`, `tags.ts`, `items.ts`
- `apps/web/src/routes/` — `__root.tsx`, `__root.module.css`, `index.tsx` (redirect to /today), `today.tsx`, `tomorrow.tsx`, `next-7-days.tsx`, `inbox.tsx`, `all.tsx`, `completed.tsx`, `trash.tsx`, `calendar.index.tsx`, `calendar.month.tsx`, `calendar.week.tsx`, `project.$id.tsx`, `project.$id.kanban.tsx`, `tag.$name.tsx`, `settings.tsx`
- `apps/web/src/components/` — `sidebar/`, plus inline placeholders for the components task 05/06 will refine
- `apps/web/src/store/` — `theme.ts`, `command-palette.ts`
- `apps/web/src/routeTree.gen.ts` (generated by TanStack Router file-based plugin)

**Files modified:**
- `apps/web/src/main.tsx` — full provider tree (ErrorBoundary, QueryClientProvider, ThemeBootstrap, HotkeyProvider, SSEConnector, SnackbarHost, CommandPaletteHost, RouterProvider)
- `apps/web/vite.config.ts` — added TanStack Router file-based plugin
- `apps/web/package.json` — added `@tanstack/router-vite-plugin` and related deps
- `biome.json` — small ignore/rule adjustments for generated files
- `pnpm-lock.yaml` — dep updates

**Files dropped (per brief):**
- `apps/web/src/app.tsx` and `apps/web/src/app.test.tsx` (from task-01) — router took over (NOTE: per `git status`, these are still on disk — implementer must delete them)

**Decisions not in brief:**
- Used `<dialog open>` (declarative) instead of `.showModal()` imperative API for the New Project modal — simpler for a React-controlled modal
- `onContextMenu` on the folder `<li>` rather than the heading — `<h2>` semantically scopes the heading content, while right-click should apply to the whole row

**Issues hit:** small typing fixes related to biome's `useSemanticElements` lint rule + React event handler types.

**Confirmed:** `pnpm --filter @tasko/web typecheck` 0 errors. `pnpm --filter @tasko/web build` succeeds. `pnpm lint` clean.

### Test
_(filled in after tester returns)_

### Review

**Verdict:** Issues found.

**Per-criterion check:** all 13 criteria pass.

**Issues:**
1. **Blocking (a11y)** — `__root.tsx:16` `<aside aria-label="Primary navigation">` wraps `sidebar/index.tsx:170` `<nav aria-label="Primary navigation">`. Two nested landmarks with identical labels. WCAG-failing. Fix: remove `aria-label` from the `<aside>` — the inner `<nav>` already satisfies the landmark requirement.
2. **Non-blocking (DRY)** — `useHealth` + `HealthSchema` defined locally at `sidebar/index.tsx:15-29`. Convention puts hooks in `api/`. Fix: move to `apps/web/src/api/health.ts`.
3. **Non-blocking (noise)** — `useDeleteProject` stub at `projects.ts:57-70` returns `Promise.reject(...)` with no `onError`, producing an unhandled-rejection console warning at runtime. Fix: add no-op `onError: () => {}`.

**Code quality:** clean elsewhere. Microcopy + ARIA + landmarks + heading hierarchy all match the spec.

**Downstream contracts:** all preserved (`apiCall<TOut>`, `ApiError`, query keys, snackbar store, command-palette store, theme bootstrap, SSEConnector + HotkeyProvider stubs).

**Test quality:** 62/62 web tests pass. Solid coverage.

**Regressions:** none. 207 tests total (62 web + 115 server + 30 types) all pass.

**Bonus deletions confirmed:** `apps/web/src/app.tsx` + `app.test.tsx` removed by orchestrator. 7 test files (was 8) now.

---

## Iteration 2

### Fix
_(pending fix-implementer)_

### Test
_(pending)_

### Review
_(pending)_
