---
status: complete
commit: 4066b40
completedAt: 2026-05-19T22:35:00Z
iterations: 3
---

# Task Completion — Task 15: Calendar week view + per-project Kanban view

**Verification:** Both views shipped. Calendar week view at `/calendar/week` — all-day strip (with multi-day continuous chips reusing the month view's `data-day-position` approach), 24-hour × 7-day time grid, `CalendarWeekBlock` component with priority left-border + click→modal (NO drag, per binding resolution §1.1), `CalendarFiltersBar` reused from task-14 (incl. "Show completed"), header `<Mon DD> – <Mon DD>, <Year>` (microcopy §24 verbatim — fixed in iter-3 from a same-month branch that omitted the second month name), chevron navigation ±7 days, `?week=` URL param. Per-project Kanban view at `/project/$id/kanban` — 3 columns (To Do / In Progress / Done), Tasks-only filter (Epics/Features excluded per §9.4 #1), `KanbanColumn` with semantic `<ul>`/`<li>` (Biome-preferred over `role="list"`/`role="listitem"`), `KanbanCard` with priority stripe + meta row + selected/dragging/overdue/completed states, dnd-kit cross-column → PATCH status + "Status: <X>." snackbar (microcopy §7), within-column drag → PATCH sort_order, drop on Done → completion sequence (recurring path reuses task-11's `useToggleComplete` for "Task completed. Next: <date>." snackbar + undo), per-column `+` opens modal with `initialProjectId` + `initialStatus` pre-filled (date empty per kanban exception), Done column 50-item slice with "Showing recent 50, [Show all]" footer (binding resolution §1.3), keyboard nav (↑↓ reorder, ←→ move column, Enter/O modal, Space/X complete), column-scoped multi-select via `useMultiSelect('kanban-column')` (cross-column DISABLED per interaction-patterns §3.3), and empty-board / empty-column states with verbatim microcopy §25.

Tests: 922/922 web pass (+89 new for this task across 11 test files: week-view (16), kanban-view (4), kanban-drag-status (4), kanban-drag-to-done (4), kanban-keyboard-arrows (5), kanban-done-overflow (7), kanban-add-task (4), kanban-empty-board (4), kanban-empty-column (5), kanban-mobile-touch (5 — includes new ⌘-click multi-select test), kanban-aria-microcopy (15 — standalone ARIA + microcopy coverage)). Typecheck + lint clean (376 files).

Dev loop took 3 iterations. Iter-1 implementer shipped both views; iter-1 tester surfaced 5 `require()`-in-ESM test bugs + 1 menu-not-opened bug (no implementation bugs) and wrote 31 new tests across 3 parallel files. Iter-2 replaced require() with `vi.mocked()`, opened the filter menu before "Show completed" assertions, folded 2 parallel files into the originals, kept the third (`kanban-aria-microcopy.test.tsx`) standalone as it adds new coverage. Iter-2 reviewer caught 3 real issues: `formatWeekRange` same-month branch omitting the second month name (microcopy §24 violation, "May 18 – 24, 2026" vs spec "May 18 – May 24, 2026") with a permissive test regex that masked it; and brief step 19's column-scoped multi-select interaction (shift-click + ⌘-click + ⌘A) was unwired — `useMultiSelect` hook never called, `<ul>` had no `onClick`. Iter-3 fixed the date format, tightened the regex, wired multi-select via `useMultiSelect(itemIds, 'kanban-column')`, and adjusted `KanbanCard.handleClick` to skip `stopPropagation()` on modifier-key clicks so the `<ul>` actually receives them. Added a `@tanstack/react-router` mock to all 10 kanban test files (the hook calls `useRouterState`).

See `log.md` for the full per-iteration execution log.
