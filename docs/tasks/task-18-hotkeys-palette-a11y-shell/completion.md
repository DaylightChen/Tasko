---
status: complete
commit: 367e4f8
completedAt: 2026-05-20T00:30:00Z
iterations: 3
---

# Task Completion — Task 18: Hotkey registry + command palette + a11y shell

**Verification:** Hotkey infrastructure + command palette + shortcut help overlay + a11y shell all shipped. Mode-aware `useHotkeyStore` with `modeStack` + 10 modes (`no-input` / `input` / `modal` / `sheet` / `calendar` / `kanban` / `tree` / `command-palette` / `tag-input` / `date-picker`). `useHotkey(mode, key, handler)` hook subscribes to currentMode; `HotkeyProvider` registers one capture-phase document keydown listener that delegates to registered handlers with mode-aware suppression. New `GlobalShortcuts` component wires the no-input single-keys per binding resolution: `t` checks `focusedRowStore.focusedId` and either reschedules the focused item to today OR navigates to `/today`; `i` → `/inbox`; `n` / `/` → focus the current view's quick-add via `quickAddRefStore`. ⌘K opens the cmdk-backed command palette with verbatim §12.1 placeholder ("Type a command…"), §12.1 empty state ("No matching commands. Try a different word."), §12.1 section headers (Recent / Navigate / Create / View / Settings), every static §12.2 command in `command-catalog.ts`, dynamic "Go to <Project>" + "Go to #<tag>" generated at palette open, and tree-context dynamic commands ("Add Epic in <project>", "Add Feature under <Epic>", "Add Task under <Feature>") when `focusedTreeContext` is supplied. `<dialog aria-modal="true">` per accessibility §3.10/§33.5. Recent commands persisted to `localStorage` under `tasko.command.recent`. Escape uses a document-level capture listener (events bubble up not down; backdrop click is mouse-only sugar). `?` toggles the floating bottom-right shortcut help overlay with verbatim §13 content. `⌘F` shows the "Use ⌘K to navigate." snackbar with a 60-second module-level debounce. `matchHotkey('?', e)` correctly handles US keyboards where `?` arrives with `shiftKey: true` (non-alphanumeric single chars skip the shift guard). Mode push/pop wired on Modal, Sheet, tag-input, date-picker, calendar-month, kanban-view, tree-view, command-palette — and the existing per-view row handlers continue to interpret their bindings under the correct mode. a11y shell verified: `<a className="skip-link" href="#main">` first focusable in `__root.tsx`, `<nav aria-label="Primary navigation">` wrapping the sidebar, `<main id="main">` wrapping route content, `<html lang="en">`, one `<h1>` per route via `ViewChrome`.

Tests: 1110/1110 web pass (+111 new across 16 task-18 test files — useHotkey, hotkey-provider, no-search-toast, command-palette (×2), shortcut-help-overlay, keyboard-help-from-palette, lib/keyboard, store/hotkey-registry/sidebar/shortcut-help, app/global-shortcuts/command-catalog/mode-suppression/a11y-shell). Typecheck + lint clean (426 files).

Dev loop took 3 iterations. Iter-1 implementer landed all 12 steps using the WRAP approach for view refactors (existing per-view handlers stay; only mode pushes added on mount/unmount). Iter-1 tester found 2 production bugs + 1 test approach issue: cmdk needs `ResizeObserver` + `scrollIntoView` stubs in jsdom setup (10 tests crashed); `matchHotkey('?', e)` rejected `shiftKey=true` events (Shift+/ produces key='?' with shiftKey=true on US keyboards, so the `?` binding was non-functional); palette Escape handler was on backdrop's onClick but tests fired on document.body (events bubble up, not down). Iter-2 added both stubs, scoped the shift guard to alphanumeric single chars, and moved palette Escape to a document-level capture listener. Iter-2 reviewer caught 5 deeper issues: the HOTKEY_MAP single-key global shortcuts (`t`/`i`/`n`/`/`) were documented but never wired to handlers; tree-context dynamic commands ("Add Epic in <project>", etc.) absent + "Move <Title> to…" never connected; missing `aria-modal="true"` on palette dialog; "View keyboard shortcuts" misgrouped in palette Navigate section; stale "task-18 will consolidate into registry" comment in tree-view with the ⌘⇧M handler still ad-hoc. Iter-3 closed all 5 — added `GlobalShortcuts` component, extended `DynamicCatalogContext` + `buildDynamicCommands`, added the `aria-modal`, split the curated View group, migrated ⌘⇧M to `useHotkey('tree', 'Mod+Shift+m', …)`.

See `log.md` for the full per-iteration execution log.
