---
status: complete
commit: 775e0d2
completedAt: 2026-05-19T21:25:00Z
iterations: 3
---

# Task Completion — Task 14: Calendar (month view) + filter chips incl. "Show completed"

**Verification:** Calendar month view shipped end-to-end. Routes (`/calendar/index` redirect → `/calendar/month`), the `<CalendarMonthView />` view, the `useCalendarItems` data hook (with optional `show_completed` merging), the 7-column × 6-row grid with `role="grid"` + per-cell `gridcell` ARIA, the `CalendarDayCell` + `CalendarEventChip` components, the three chip variants (timed / all-day / multi-day with `data-day-position={start|middle|end}`), priority left-border, overdue treatment, completed treatment (strike-through + opacity 0.6), the day-detail popover with `aria-modal="false"` and footer "Add task on <Mon DD>" (verbatim §24 microcopy, no "+ " prefix), the `+ Filter` dropdown with project/tag sub-pickers + "Show completed" toggle driving URL search params (`filter_project_id`, `filter_tag_id`, `show_completed`), filter chips per §29, "Clear all" link, "+N more" overflow with `aria-haspopup="dialog"` + `aria-expanded` toggling, keyboard nav (←→ day, ↑↓ week, PgUp/PgDn month, Shift+PgUp/PgDn year, Home/End week, T today, Enter day-detail, N new-task with date pre-filled), empty state with verbatim §24 microcopy, and the no-drag invariant (no `@dnd-kit` import anywhere in calendar code — drag-to-reschedule is CUT per binding resolution §1.1, documented in `docs/known-issues.md`).

Tests: 833/833 web pass (+91 new for this task across 8 test files: month-view 29, month-keyboard-nav 13, month-filter-chips 11, day-detail-popover 10, month-week-start 4, month-empty 4, month-overdue-render 4, calendar-no-drag 6). Typecheck + lint clean (355 files).

Dev loop took 3 iterations. Iter-1 implementer landed all 11 steps; tester surfaced 17 test-bug failures (zero-item seeding, incomplete `date-fmt` mock, vitest hoisting trap, jsdom `DragEvent` not defined, URL-state leak between tests) AND two real implementation gaps (the `+ Filter` dropdown only had "Show completed" — missing "Filter by project…" + "Filter by tag…" sub-options per brief step 3; the day-detail "Add task on…" button had a "+ " prefix not in microcopy §24). Iter-2 wired the project/tag sub-pickers via an inline back-button sub-menu (drives `handleSelectProject` / `handleSelectTag` → `onFiltersChange` → URL params), removed the prefix, repaired all 6 broken test files, and folded the tester's 5 parallel "_fixed"/"_expanded" files into the 8 brief-listed ones. Iter-2 reviewer then found 6 more real spec gaps: missing `aria-expanded` on "+N more" button (§3.12.7), "Show: completed" chip label with an inner colon (microcopy §24 calls for "Show completed", no colon), `isEmpty` checking the full `items.length` instead of the visible-grid intersection (brief §8), `CalendarFiltersBar` not exported (task-15 explicitly depends on reuse), `formatDayDetailTitle` using `MONTH_LONG` instead of `MONTH_SHORT` (only May coincidentally rendered correctly), and missing `aria-modal="false"` on the day-detail `<dialog>` (anchored, not full modal). Iter-3 closed all 6 — adding the `isDetailOpen` prop wiring, a `label?: string` override on `FilterChip` (backward-compatible), the `hasItemsInGrid` semantic fix, the exports, the `MONTH_SHORT` swap + January regression test, and the dialog ARIA. The orchestrator caught two follow-on issues inline: a defensive `if (moreBtn)` conditional in the January test (could false-pass; tightened to unconditional `screen.getByText`), and a regression in `month-keyboard-nav.test.tsx` caused by fix-3's stricter empty-state logic (1-item mock collapsed the grid after PageDown; widened to 5 items across 2025-2027).

See `log.md` for the full per-iteration execution log.
